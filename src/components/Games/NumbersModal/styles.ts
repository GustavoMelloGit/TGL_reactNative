import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  numbersWrapper: {
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-evenly",
    marginBottom: 160,
  },
});
