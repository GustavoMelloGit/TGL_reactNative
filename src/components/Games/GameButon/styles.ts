import { StyleSheet } from "react-native";

export const GameButtonStyles = StyleSheet.create({
  button: {
    borderRadius: 100,
    borderWidth: 1,
    width: 100,
    height: 30,
    justifyContent: "center",
    alignItems: "center",
    margin: 3,
  },
  text: {
    fontSize: 15,
  },
});
